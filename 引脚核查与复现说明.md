# 本车引脚核查与两组程序复现说明

核查日期：2026-09-06。依据本仓库 `引脚对应表2.xlsx`、所附中文《ESP32-S3-WROOM-2 技术规格书》v1.7，以及两组源代码。模组配置按仓库此前记录的 **ESP32-S3-WROOM-2-N32R16V** 处理：32MB Octal Flash、16MB Octal PSRAM。开发板的通用名称“DevKitC-1”不能代替模组具体型号。

**当前白屏排查：VScode ESP 临时使用 LCD CS=GPIO1，须断电拔掉 GPIO1 的水平舵机信号线，再把屏幕 CS 从0移到1。** Excel 与 camera-claude 保留 CS=GPIO0、水平舵机=GPIO1，切回时须恢复接线。GPIO1 测试方案只适用于当前未启用舵机的 VScode ESP，不能与舵机共用。

此前已完成按表适配：VScode ESP 只启动巡线和超声波避障；camera-claude 默认恢复原组完整模式6，并修复原移植版速度闭环未实际启用的问题。

## 当前对应表

以下数字全部表示GPIO号，不能当成模组焊盘序号。轮子位置来自现有 `car-spin/README.md`：A右前、B后方、D左前；Excel只写接头名，没有写轮位。

| Excel 信号 | 当前 GPIO | VScode ESP 对应 | camera-claude 对应 |
| --- | --- | --- | --- |
| PWMA / AIN1 / AIN2 | 9 / 12 / 10 | M3 PWM / INA / INB，右前 | A轮 |
| PWMB / BIN1 / BIN2 | 4 / 6 / 5 | M2 PWM / INA / INB，后轮 | B轮 |
| PWMD / DIN1 / DIN2 | 16 / 7 / 15 | M1 PWM / INA / INB，左前 | D轮 |
| STBY | 8 | 新增初始化与使能 | 已有STBY使能 |
| E1A / E1B | 39 / 40 | M3编码器输入 | A轮PCNT |
| E2A / E2B | 17 / 3 | M2编码器输入 | B轮PCNT |
| E4A / E4B | 41 / 42 | M1编码器输入 | D轮PCNT |
| PAN_SIG / TILT_SIG | 1 / 2 | 无舵机驱动；GPIO1 信号线拔掉，临时给 LCD CS | 水平/俯仰舵机 |
| USB_D- / USB_D+ | 19 / 20 | USB Host，固定 | USB Host，固定 |
| Trig / Echo | 18 / 11 | 超声波避障 | 超声波避障 |
| LCD SCK / SDI | 13 / 14 | SPI2，10MHz | SPI2，10MHz |
| LCD D/C | 21 | LCD DC | TFT DC |
| LCD CS | Excel为0，原表47 | **GPIO1，临时排查** | GPIO0 |
| LCD RST | 38 | LCD RST | TFT RST |
| UART TX0 / RX0 | 43 / 44 | 日志/烧录口 | 日志/烧录口 |

VScode ESP 的可改脚号集中在 `esp-projects/VScode ESP/VScode ESP/main/board_pins.h`；camera-claude 的可改脚号集中在 `esp-projects/camera-claude/main/board_pins.h`。

## 引脚表的核查结论

### 1. GPIO47/48 是本表最需要修改的地方

所附中文数据手册**第11页、表3-1脚注2**明确说明：ESP32-S3R8V/R16V 的 VDD_SPI 已设为1.8V，GPIO47/48同属该电压域。原表把3.3V屏幕的CS接到47，不能默认满足逻辑电平要求：1.8V高电平未必被屏幕识别；若屏幕板把CS上拉到3.3V，还会使该脚承受超出其电压域的电平。“曾经能显示”不能证明电气兼容。

此前按确认方案改为GPIO0，并同步Excel的 `Sheet1!A51`；白屏排查期间仅VScode ESP暂改GPIO1，详见本文开头。Excel/camera-claude保留的GPIO0方案需要满足以下复位条件：

- LCD CS是高阻输入，正常上电/复位时不能由屏幕拉低；可用约10kΩ上拉到3.3V。软件初始化之后才设置上拉，不能补救已经发生的启动采样。
- GPIO0同时连BOOT，正常从Flash启动须为高；进入下载模式时主动拉低是预期操作。不要在正常运行中按BOOT，按下会同时影响屏幕CS。
- 若屏幕CS带无法去掉的下拉，GPIO0方案就不适合该屏幕板。单屏独占SPI时的替代方案是CS只接GND，并把两工程对应CS宏设为`-1`；这不是当前默认接线。

依据：中文数据手册第12–14页、表4-1/4-2/4-3。GPIO0在EN拉高后的strapping保持时间至少3ms内须满足启动电平。此项是硬件接线要求，编译通过不能验证它。

### 2. 现有电机、编码器、超声波和舵机脚号可保留

当前启用外设没有重复GPIO，均位于WROOM-2实际引出范围。camera-claude全套外设用27个GPIO，加UART0为29个；剩余45、46是strapping脚，47、48是1.8V域，已经没有不带这些限制的普通空闲GPIO。VScode ESP没有驱动舵机，GPIO1临时借给LCD CS、GPIO2不配置；GPIO1原舵机信号线必须拔掉，不能同时接两个外设。

**GPIO3有条件可用。** 它是JTAG源选择的strapping脚，不能仅凭“默认浮空”认定无条件安全。中文数据手册第14页表4-5表明：出厂默认的相关eFuse组合忽略GPIO3电平，可以保留作E2B。若曾改变JTAG相关eFuse，需要重新按表核对。GPIO39~42可作为PCNT输入；此时不能再同时使用这些线做外部四线JTAG。本次没有读取或烧写硬件eFuse。

**GPIO45/46并非一律不能用。** 默认弱下拉只描述复位状态；strapping采样后可作普通GPIO。GPIO46在GPIO0高的正常Flash启动下允许任意值，在GPIO0低的联合下载模式下须低。GPIO45是否控制VDD_SPI还受eFuse影响；不能为了尝试外设随意改变Flash/PSRAM电压。由于有这些条件，本次没有把其他信号挪到45/46。

### 3. 不要照其他S3开发板的图使用GPIO35/36/37

中文数据手册**第9–11页、图3-1/表3-1**给出本模组只引出GPIO0–21与38–48，共33个GPIO；模组焊盘28/29/30是NC。GPIO22–25不存在于S3的GPIO编号范围，GPIO26–37涉及内部存储接口或未在此模组引出，均不能作为本车的新外设接线。

尤其要区别“模组序号26”和“GPIO26”：本模组焊盘26对应GPIO45。不能混淆焊盘序号、开发板排针位置和GPIO号。

### 4. 屏幕GPIO13/14可以用，但原表的描述不够准确

GPIO13不是SPI2原生时钟脚，GPIO14也不是SPI2原生MOSI；原生FSPICLK是GPIO12、FSPID/MOSI是GPIO11。ESP-IDF支持把硬件SPI经GPIO Matrix映射到其他可用脚，所以13/14仍是硬件SPI驱动，可以继续使用。

已改正 `Sheet1!B48/B49` 的说明。没必要为了换回原生IO_MUX而占用本车AIN1/ECHO。VScode ESP已从26MHz降至10MHz，camera-claude也是10MHz，尚未实车验证；芯片路由允许某频率不等于具体屏幕、杜邦线长度在该频率必然可靠。依据：[ESP-IDF 5.4.4 SPI主机文档](https://docs.espressif.com/projects/esp-idf/en/v5.4.4/esp32s3/api-reference/peripherals/spi_master.html#gpio-matrix-and-io-mux)。

### 5. USB与UART分配正确，供电与逻辑电平还要分别核对

USB D-=GPIO19、D+=GPIO20固定，不能为了腾GPIO随意调换。相机占用USB PHY后，用UART0的GPIO43/44持续看日志；Excel `A42/A43` 已从笼统TX/RX明确为GPIO43/TX0和GPIO44/RX0，仍是TX接对方RX、RX接对方TX，并共地。依据中文数据手册第10–11页以及第20–21页USB说明。

所附摄像头接口图片明确标明**5V供电**。相机VBUS供电与D+/D-信号不是同一回事；不能把5V接入GPIO。串口逻辑需3.3V；若主板和USB转串口模块分别供电，通常只接TX/RX/GND，不直接并联两个电源输出。

超声波 `C35=3.3V` 暂保留并增加批注：具体型号未知，不能仅凭ESP32数据手册判断它能否以3.3V工作。若实物为要求5V的普通HC-SR04，应按其规格供电；若Echo输出5V，必须电平转换/分压后再接GPIO11。3.3V兼容变体须看实物规格或测量，不能一概当作5V输出。

编码器输出也应为本模组接受的3.3V逻辑，不可仅因驱动板带3.3V插座就推断每路输出电平。舵机的GPIO1/2只是控制信号，舵机电源按其规格接稳压供电并共地。中文数据手册第25页表6-2建议模组3.0–3.6V供电、供电能力至少0.5A；这不是整车加电机/舵机的总预算。驱动板附图的3.3V输出标称0.5A，不能据此默认足以同时带主控和所有负载。

### 6. 表中的空白项尚未分配，不能按旧版接线混接

ADC、四路红外、温湿度DATA、陀螺仪INT/SDA/SCL等没有GPIO，本次没有替它们虚构接线。现在两组摄像头程序不需要这些信号。GPIO17已经给E2A，不能再连旧的电压ADC；旧红外41/42/2/1会与编码器和云台冲突。

以后要同时增加多个传感器，需要先明确需求，再考虑I/O扩展或释放某些外设。不是把空白项填上任意空闲数字就能工作。

## 两组代码具体改动

### VScode ESP

按几何位置映射 **M1→D、M2→B、M3→A**，补GPIO8 STBY，并将现有/默认配置显式设为本车的32MB Octal Flash、OPI DTR 80MHz。Octal PSRAM 80MHz与CPU 240MHz保留。

按后续明确要求只启用 `detect_line_from_rgb565`、`overlay_detected_line`、`line_follow_task`、`avoid_task` 和 `ultrasonic_init`。找球检测、找球叠加、找球运动任务均在`#if 0`内，不编译、不执行。原组巡线/避障算法与参数未重写。

保留原组电机方向系数`{1,1,-1}`。直行算法给M1负、M3正，乘方向系数后D/A均为IN1低、IN2高，与本仓库旧接线说明相符；实车安装/电机插头变化仍需架空确认。该组原来没有云台控制，也没有编码器速度闭环，改引脚不会自动增加这两项功能。

原`build`是另一台电脑生成的缓存与旧固件；用本工程README中的`build-local`重新构建，不能直接烧原有`.bin`。原配置启用了Flash自动识别，所以不能断言DIO字样本身就是之前崩溃的根因；显式OPI是为了与已知模组一致。OPI下IDF生成`CONFIG_ESPTOOLPY_FLASHMODE="dout"`是正常的镜像头约定，见[ESP-IDF Kconfig源码](https://github.com/espressif/esp-idf/blob/v5.5.5/components/esptool_py/Kconfig.projbuild)。

### camera-claude

除LCD CS外，原有外设映射已经与Excel一致。但它此前并非“只改脚号的原版”：默认模式8、不执行完整运动流程；PWM上限由8191改成3604；`pid.c`又缺少`board_pins.h`，使`USE_ENCODER`在PID中不可见，实际跳过了速度闭环。

现在恢复模式6和8191上限，并补入头文件使编码器/闭环同时启用。原组PID、视觉、避障、推球参数保留，模式7/8仅供手动诊断。仍保留已有的STBY适配、编码器2048字节任务栈、红外冲突保护和为完整ESP-IDF工程补齐的配置。原组未提供完整sdkconfig及组件锁，不能声称构建环境与他们逐字相同。

此前README里“闭环会跨周期一路累加到上限”“47用在屏幕上已排除问题”“45/46上电必须低”“所有启动步骤OK即可证明启动无问题”等推断不成立，说明已修正。此前的重启现象没有新串口日志和实机测试，不能宣布已解决。

## 明天的验证顺序

1. 按要运行的工程接LCD CS：VScode ESP当前临时接GPIO1，并拔掉GPIO1的舵机信号线；camera-claude仍接GPIO0并满足BOOT复位电平要求。核对地线、相机5V和超声波Echo电平，清掉与当前方案冲突的旧接线。
2. 进入要尝试的工程内层目录，用其README命令重新构建；选择UART串口烧录并保存启动日志。不要把工作区原来指向的`camera-test`当成此次项目。
3. 架空检查屏幕、轮位与转向；VScode ESP应出现摄像头黑线标记、`line:`和`us dist=`日志。camera-claude的TFT主要显示距离/编码器，画面预览走Wi-Fi。
4. 再落地验证巡线与避障。两组当前都不能保证拔摄像头后停车，用电源开关停止。VScode原避障任务可以独立测距触发，不能笼统保证“第一帧前所有任务都不动电机”。

## 验证记录与边界

- 已逐项核对Excel和两个引脚头文件，确认启用外设没有重复GPIO、不使用未引出的脚或GPIO47/48，USB与UART分配正确。
- 已用C预处理验证：camera-claude模式6主程序与启用编码器后的PID，有效token与原组对应源文件一致；VScode ESP的有效代码只保留巡线/避障路径，排除了找球。
- 已比较VScode原始/当前的黑线检测、黑线叠加、巡线、避障、横移、电机速度函数：算法及常量保持一致，避障日志中的Echo号随映射变化。
- **没有完整ESP-IDF编译、烧录或实车验证。** 当前机器未发现可用的ESP-IDF/ESP32交叉编译环境，目录内已有构建文件来自原电脑。主机C预处理不验证ESP-IDF API、链接、内存占用、eFuse、线序电平或电机实物方向。

主要硬件证据为本地中文数据手册v1.7；另用[乐鑫官方同版英文数据手册](https://www.espressif.com/sites/default/files/documentation/esp32-s3-wroom-2_datasheet_en.pdf)和[ESP-IDF GPIO文档](https://docs.espressif.com/projects/esp-idf/en/v5.4.4/esp32s3/api-reference/peripherals/gpio.html)交叉核对。本文页码均指所附中文版，英文版分页不同。
