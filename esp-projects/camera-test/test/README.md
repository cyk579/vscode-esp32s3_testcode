# 巡线几何层的离线回归测试

本机没有 `idf.py`，所以 ESP 侧代码无法在这里烧写前编译。这个目录提供两条
在 PC 上就能跑的验证：

```sh
cd test
sh build.sh          # 或者 mingw32-make run && mingw32-make syntax
```

1. **合成帧回归**：`host_harness.c` 用 gcc 直接编译 `main/line_geometry.c`，
   在 240×160 的合成帧上跑七个场景（居中直线、偏移直线、左斜线、左/右 90°
   弯、10 cm 横杆终点 T、深红色球干扰），每个场景都在 7/11/15 px 三种线宽下
   各跑一遍。断言写的是**正确行为**，所以在几何层修好之前是红的。
2. **ESP 侧语法检查**：`esp_stubs/` 里是最小的 ESP-IDF 桩头文件，只为了让
   host gcc 能对 `camera_line_follow.c` 做 `-fsyntax-only`，`LINE_CALIB_MODE`
   的两条分支都会编一遍。它不验证行为，但能挡住类型错误和笔误。

3. **混控与控制律单元测试**：`line_mixer` 的等比缩放/起转值处理/反解，以及
   `line_control` 的符号和抖动平均值。`sign-*` 用例把整条链路（几何 → 控制律
   → 混控 → 车体速度）钉住 —— 转向和平移是两个不同的执行器，和误差符号的
   关系不一样，这里已经抓到过一次符号写反。

## 摄像头装反 90 度怎么判、怎么改

扫描坐标系永远是**车体视角**：`sy` 越大越靠近车。缓冲区按
`CAMERA_LINE_ROTATION` 反查，不做整帧旋转拷贝，所以改它不增加单帧耗时。

判断方法（任选其一，第一条最不容易看错）：

1. **看串口日志**。装反 90 度时，逐行扫描是横切胶带的：每行要么整行落在胶带
   里（黑段占满搜索窗口 → 判成双侧宽 → `finish`），要么整行都在胶带外（没有
   黑段 → 漏行）。所以 `valid_rows` 会一直是 0~3、`state` 在 LOST 和 FINISH
   之间反复，`line_w` 也会异常大（量到的是胶带的**长度**而不是宽度）。
   方向正确时 `valid_rows` 应该在 15~27，`state=NORMAL`。
2. **看 TFT 绿点**。绿点必须**沿着**胶带走。如果绿点**横切**胶带，就是装反了。
3. **看红十字**。它画在最近扫描行上，应该落在画面里离车最近的那条边上。

改法：把 `camera_line_follow.c` 的 `CAMERA_LINE_ROTATION` 改成
`LINE_ROTATE_90` / `_180` / `_270` 之一。`rot*` 回归用例证明了四个方向读出
的观测结果**逐字段完全相同**，所以这是一个安全的一行改动，不需要重写几何。

注意：TFT 面板本身是按 landscape（MADCTL 0xA0）驱动的，如果模块被物理竖装，
预览看起来就是转过 90 度的 —— 但那只影响你的眼睛，不影响算法读的缓冲区。
**预览转了 ≠ 缓冲区转了**，必须用上面三条之一区分。

## 用真实帧回放

```sh
py -3.13 player.py --host 192.168.4.1 --save-dir caps --frames 300
cd test && ./harness ../caps/frame_00042.bin
```

`player.py --save-dir` 会同时存 `.jpg` 和一份按固件同样尺寸缩放的大端
RGB565 `.bin`。回放模式只打印观测结果，不做断言 —— 真实帧的期望值需要人眼
配合 TFT 预览判断。

## 合成帧能测和不能测的

- **能测**：ROI 与逐行搜索的几何、线段分类（正常/单侧宽/双侧宽）、误差符号
  与标度、拐角与终点的区分、状态语义。
- **不能测**：真实光照梯度、反光、JPEG 噪声、镜头畸变、胶带边缘的抗锯齿、
  实际帧率与控制延迟。这些必须用真机采帧回放，或者直接上车。
